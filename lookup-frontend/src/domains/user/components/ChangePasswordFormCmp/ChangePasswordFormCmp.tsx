import { FC } from "react";

interface ChangePasswordFormCmpProps {
  close: () => void;
}

const ChangePasswordFormCmp: FC<ChangePasswordFormCmpProps> = ({ close }) => {
  return <>ChangePasswordFormCmp</>;
};

export default ChangePasswordFormCmp;
